import React from "react";
import "./WarningPage.scss";

export const WarningPage: React.FunctionComponent = () => {
  return (
    <p className="warning">
      You might have to log in to add the propduct to wish list or cart!!
    </p>
  );
};
